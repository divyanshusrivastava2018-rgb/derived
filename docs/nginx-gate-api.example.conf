# Example: proxy GATE exam API to Node (port 3000) on derived.co.in
# Place inside your server { } block after ssl config.

location /api/ {
    proxy_pass http://127.0.0.1:3000;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
}

location /healthz {
    proxy_pass http://127.0.0.1:3000;
    proxy_set_header Host $host;
}

# Optional: serve static files from public/ via nginx, or let Node serve everything:
# location / {
#     root /var/www/derived-main/public;
#     try_files $uri $uri/ /404.html;
# }
